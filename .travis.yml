sudo: required

services:
  - mysql
  - postgresql
language: python
python:
  - 3.5.0
env:
  - DJANGO=1.9
#install: // Install section is empty ATM, waiting for requirement analysis and tests
before_script:
  - psql -c 'CREATE DATABASE Vigilate;' -U postgres
  - echo "USE mysql;\nUPDATE user SET password=PASSWORD('toor') WHERE user='root';\nFLUSH PRIVILEGES;\n" | mysql -u root
  - mysql -e 'create database Vigilate;'
  - sudo apt-get -qq update
  - sudo apt-get install -y python3-dev
  - sudo ./install-pyargon2.sh
script:
  - python manage.py test scanner_api/